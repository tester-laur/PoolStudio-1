@model IEnumerable<PoolStudio.WEB.Models.Clasification>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ItemType)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ItemType)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ClasificationId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ClasificationId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ClasificationId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
